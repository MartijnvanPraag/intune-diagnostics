{
  "name": "intune-diagnostics",
  "version": "0.1.0",
  "description": "Modern web app for Intune diagnostics with Agentic AI",
  "scripts": {
    "dev": "concurrently --name backend,frontend --prefix name --restart-tries=100 --restart-after=2000 \"npm run backend:dev\" \"npm run frontend:dev\"",
    "backend:dev": "cd backend && uv run uvicorn main:app --host 0.0.0.0 --port 8000",
    "frontend:dev": "cd frontend && npm run dev",
    "frontend:install": "cd frontend && npm install",
    "backend:install": "uv sync",
    "install": "npm run backend:install && npm run frontend:install",
    "build": "cd frontend && npm run build",
    "build:all": "npm run frontend:install && npm run build",
    "start": "cd backend && uv run uvicorn main:app --host 0.0.0.0 --port 8000",
    "start:prod": "cd backend && uv run gunicorn main:app --workers 4 --worker-class uvicorn.workers.UvicornWorker --bind 0.0.0.0:8000"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}